# Python Dependencies for Pioneer Multi-Robot Testbed
# Install with: pip3 install -r requirements.txt

# Core numerical and data tooling
numpy>=1.20.0
sympy>=1.9
pandas>=1.3.0
matplotlib>=3.3.0

# GUI and visualization
PyQt6>=6.0.0

# Robotics and hardware integration
pyserial>=3.5
psutil>=5.8.0
PyYAML>=5.4.0
pyproj>=3.0.0
numpy-stl>=2.16.0

# XBee communication (optional, for RF communication)
# digi-xbee>=1.4.0

# Adafruit sensors (optional, for hardware sensors)
# Adafruit-Blinka>=8.0.0
# adafruit-circuitpython-bno055>=5.0.0
# adafruit-circuitpython-gps>=3.0.0

# Testing
pytest>=6.2.0

# ROS 2 Dependencies (install via apt, NOT pip)
# These are provided by ROS 2 Jazzy installation:
# - rclpy
# - rosidl-runtime-py
# - geometry-msgs
# - sensor-msgs
# - std-msgs
# - nav-msgs

# System Dependencies (install via apt)
# sudo apt install -y python3-pip python3-dev
# sudo apt install -y ros-jazzy-desktop
# sudo apt install -y ros-jazzy-xacro
# sudo apt install -y ros-jazzy-joint-state-publisher-gui
